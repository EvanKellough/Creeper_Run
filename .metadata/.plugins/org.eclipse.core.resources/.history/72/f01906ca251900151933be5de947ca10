package ;

import java.io.IOException;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.PrintWriter;

public class main {
	
	private String path;
	private boolean append_to_file = false;
  
  public main(String file_path){
	  path = file_path;
  }

  public main(String file_path, boolean append_value ){
	  path = file_path;
  }
  
  FileWriter write = new FileWriter(path, append_to_file);
  
  print_line.printf("%s" + "%n", textLine);
  
  print_line.close();

	  	String map = "C:/cartoon.txt";
	  	/*
	  	try{
	  		ReadFile file = new ReadFile(map)
	  		String[] aryLines = file.OpenFile();
	  		int i;
	  		for (i = 0; i < aryLines.length; i++){
	  			System.out.println(aryLines[i]);
	  		}
	  	}
	  	catch (IOException e){
	  		System.out.println("Didn't work :'(");
	  	}
  }
  */
  //write to a file
  main data = new main(map, true);
  int x = 152;
  int y = 96;
  
  //data.writeToFile(null, x + " " + y + " ");
  data.writeToFile("Text");
  
  int count = 0 ;
  while ( count <= x )
  {
	 if (count = x){
		 count = 0;
	 }
	 data.writeToFile(count + " ");
     count++ ;
  }
  
  System.out.println("New Map Created! Go You!");
  }
}